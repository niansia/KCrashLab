# Driver track (not implemented in v1)

This directory is intentionally documentation-only. KCrashLab v1 does not contain or build a vulnerable kernel driver. Track B work is blocked until the lab gates in `docs/lab-safety.md` pass.

